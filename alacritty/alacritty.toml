[env]
TERM = "xterm-256color"

[shell]
program = "bash"
args = ["-lc", "exec \"${XDG_CONFIG_HOME:-$HOME/.config}/tmux/tmux_setup.sh\""]

[window]
padding.x = 16
padding.y = 14
decorations = "None"
opacity = 0.98

[keyboard]
bindings = [
{ key = "F11", action = "ToggleFullscreen" }
]
